spring:
  config:
    activate:
      on-profile: local

  datasource:
    url: jdbc:mysql://fitpath.cg7ys4qcefrj.us-east-1.rds.amazonaws.com:3306/fitpath?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: admin            # <-- your local MySQL user
    password: "THISisaPASSWORD8"     # <-- your local MySQL password
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
    show-sql: true

  mail:
    host: smtp.gmail.com
    port: 587
    username: leoyegh@gmail.com
    password: "aclg lrsu owpu tdkh"
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

server:
  port: 8080
  error:
    include-message: always
    include-binding-errors: always
